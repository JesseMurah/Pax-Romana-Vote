export declare class PhoneValidationUtil {
    static normalizeGhanaPhone(phoneNumber: string): string;
    static isValidGhanaPhone(phoneNumber: string): boolean;
    static formatForDisplay(phoneNumber: string): string;
}
