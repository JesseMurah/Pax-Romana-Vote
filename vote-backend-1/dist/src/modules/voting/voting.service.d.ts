import { ConfigService } from "@nestjs/config";
import { PrismaService } from "../../../db";
import { CacheService } from "../caches/cache.service";
import { HttpService } from '@nestjs/axios';
import { GenerateOtpDto } from "./dto/generate-otp.dto";
import { VerifyOtpDto } from "./dto/verify-otp.dto";
import { SubmitVoteDto } from "./dto/submit-vote.dto";
import { RealTimeService } from '../real-time/services/real-time.service';
import { VotingStatsService } from '../real-time/services/voting-stats.service';
import { AnomalyDetectionService } from '../real-time/services/anomaly-detection.service';
import { SseEventType } from '../real-time/enums/sse-event-types.enum';
import { UserRole } from '@prisma/client';
import { Candidate_Position } from "@prisma/client/index";
export declare class VotingService {
    private readonly httpService;
    private readonly configService;
    private readonly prisma;
    private readonly cacheService;
    private readonly realTimeService;
    private readonly votingStatsService;
    private readonly anomalyDetectionService;
    private readonly encryptionKey;
    constructor(httpService: HttpService, configService: ConfigService, prisma: PrismaService, cacheService: CacheService, realTimeService: RealTimeService, votingStatsService: VotingStatsService, anomalyDetectionService: AnomalyDetectionService);
    generateOtp(dto: GenerateOtpDto): Promise<any>;
    verifyOtp(dto: VerifyOtpDto): Promise<any>;
    getBallot(): Promise<any>;
    submitVote(dto: SubmitVoteDto): Promise<any>;
    getVotingStats(): Promise<any>;
    validateSession(sessionId: string): Promise<any>;
    refreshAndBroadcastStats(): Promise<void>;
    getRealtimeConnectionInfo(): Promise<any>;
    broadcastMessage(message: string, role: UserRole, type?: SseEventType): Promise<void>;
    getPositionStats(position: Candidate_Position): Promise<any>;
    getVotingVelocity(): Promise<any>;
    getVotingAnalytics(options: {
        timeframe: string;
        position?: Candidate_Position;
        requestedBy: string;
    }): Promise<any>;
    private groupVotesByHour;
    getSystemHealth(): Promise<any>;
    private checkCacheHealth;
    getAnomalies(): Promise<any>;
    pauseVoting(reason: string, pausedBy: string): Promise<void>;
    resumeVoting(reason: string, resumedBy: string): Promise<void>;
    getActiveSessions(): Promise<any>;
    exportVotingData(options: {
        format: string;
        includePersonalData: boolean;
        exportedBy: string;
    }): Promise<any>;
    private generateCSVExport;
    getVotingTimeline(): Promise<any>;
    getPublicDashboardData(): Promise<any>;
}
